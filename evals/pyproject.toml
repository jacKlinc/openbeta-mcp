[project]
name = "evals"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "mcp[cli]>=2.0.0",
    "pandas>=2.3.3",
    "tiktoken>=0.13.0",
]

[dependency-groups]
dev = [
    "mlflow[genai]==3.5.0",
    "protobuf<5",
]
