query FindArea($filter: Filter, $limit: Int) {
  areas(filter: $filter, limit: $limit) {
    uuid
    pathTokens
    areaName
    shortCode
    totalClimbs
    metadata {
      lng
      lat
    }
  }
}